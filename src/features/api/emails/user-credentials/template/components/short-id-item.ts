const shortIDItem = (shortId: string) => {
  return `<li style="box-sizing: border-box; margin-left: 4px; margin-right: 4px; border-radius: 8px; background-color: rgba(242, 227, 64, 0.5); padding: 4px 8px">${shortId}</li>`;
};

export default shortIDItem;
